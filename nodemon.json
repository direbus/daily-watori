{
  "verbose": true,
  "execMap": {
    "ts": "node --require ts-node/register --experimental-vm-modules",
    "js": "node --experimental-vm-modules"
  },
  "env": {
    "NODE_ENV": "development"
  },
  "watch": [
    "packages/**/*.ts"
  ],
  "ext": "ts,js,json"
}
